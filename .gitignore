.venv/
__pycache__/
*.pyc
train/
Test_NoisyLR/
train.zip
Test_NoisyLR.zip
NAFNet-main/
NAFNet-main.zip
runs/
outputs_*/
# submission deliverable: the restored test-set outputs must ship in the repo,
# so this one is re-included despite the outputs_*/ rule above
!outputs_final/
*.log
# training/ablation logs are kept as evidence for the numbers quoted in the
# README, so the logs/ directory is re-included despite the *.log rule above
!logs/
!logs/*.log

.claude/
Devens_gt_triptych.png

Screenshot*.png

*.pth

Extra_DataSet(Kaggle)/
Extra_DataSet(Kaggle).zip
test_single_out/
